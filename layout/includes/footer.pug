#footer-wrap
  if theme.footer.owner.enable
    - const currentYear = new Date().getFullYear()
    - const sinceYear = theme.footer.owner.since
    .copyright
      if sinceYear && sinceYear != currentYear
        != `&copy;${sinceYear} - ${currentYear} By ${config.author}`
      else
        != `&copy;${currentYear} By ${config.author}`
  if theme.footer.copyright.enable
    - const v = theme.footer.copyright.version ? getVersion() : null
    .framework-info
      span= _p('footer.framework') + ' '
      a(href='https://hexo.io')= v ? `Hexo ${v.hexo}` : 'Hexo'
      span.footer-separator
      span= _p('footer.theme') + ' '
      a(href='https://github.com/jerryc127/hexo-theme-butterfly')= v ? `Butterfly ${v.theme}` : 'Butterfly'
  if theme.footer.custom_text
    .footer_custom_text!= theme.footer.custom_text
